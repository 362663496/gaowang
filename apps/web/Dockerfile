FROM node:22-alpine

EXPOSE 3000

CMD ["node", "-e", "require('node:http').createServer((_req,res)=>{res.writeHead(200,{'content-type':'text/plain; charset=utf-8'});res.end('Gaowang web app is not scaffolded yet.');}).listen(3000,'0.0.0.0')"]
